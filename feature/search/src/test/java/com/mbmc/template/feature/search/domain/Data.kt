package com.mbmc.template.feature.search.domain

import com.mbmc.template.feature.search.domain.entity.RepoDomain

object Data {
    val REPOS = listOf(
        RepoDomain("Android-ViewPagerIndicator"),
        RepoDomain("androidthings-imageclassifier"),
        RepoDomain("FiInfo"),
        RepoDomain("mapbox-directions-android"),
        RepoDomain("MaterialEditText"),
        RepoDomain("MaterialViewPager"),
        RepoDomain("Parcel"),
        RepoDomain("RoboTest"),
        RepoDomain("Template")
    )
}